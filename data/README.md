# Data

The notebooks generate synthetic data by default so the repository is reproducible and does not depend on private datasets.

Later, this folder can be used for small public datasets or generated CSV/Parquet files.
